when:
  - event: [push, pull_request]
    branch: [master, main]

steps:
  instrumented-tests:
    image: localhost:5000/ci-theming-jdk23:latest
    privileged: true
    environment:
      ANDROID_HOME: /opt/android-sdk
      ANDROID_SDK_ROOT: /opt/android-sdk
      ANDROID_AVD_HOME: /root/.android/avd
      GRADLE_USER_HOME: /root/.gradle
      DISPLAY: ":99"
    commands:
      - |
        export PATH="$ANDROID_HOME/platform-tools:$ANDROID_HOME/emulator:$PATH"
        mkdir -p ~/.gradle
        echo "githubUser=${GPR_USERNAME}" >> ~/.gradle/gradle.properties
        echo "githubToken=${GPR_TOKEN}" >> ~/.gradle/gradle.properties
      # Install Android SDK
      - |
        if [ ! -d "$ANDROID_HOME/cmdline-tools/latest" ]; then
          mkdir -p $ANDROID_HOME/cmdline-tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O /tmp/cmdline-tools.zip
          unzip -q /tmp/cmdline-tools.zip -d $ANDROID_HOME/cmdline-tools
          mv $ANDROID_HOME/cmdline-tools/cmdline-tools $ANDROID_HOME/cmdline-tools/latest
        else
          echo "Android SDK cached, skipping install"
        fi
        echo "sdk.dir=$ANDROID_HOME" > local.properties
      # Create AVD
      - |
        if [ ! -f "$ANDROID_AVD_HOME/test.avd/config.ini" ]; then
          mkdir -p $ANDROID_AVD_HOME/test.avd
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "system-images;android-34;default;x86_64" > /dev/null 2>&1
          echo "no" | $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager create avd -n test -k "system-images;android-34;default;x86_64" --force
        else
          echo "AVD config cached, skipping creation"
        fi
      - $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platforms;android-36" "build-tools;36.0.0" > /dev/null 2>&1 || true
      # Start emulator
      - |
        Xvfb :99 -screen 0 480x800x24 &
        sleep 2
        $ANDROID_HOME/emulator/emulator -avd test -no-window -gpu swiftshader_indirect -no-audio -no-boot-anim -memory 4096 -no-snapshot -no-metrics -partition-size 4096 -read-only &
        $ANDROID_HOME/platform-tools/adb wait-for-device
        timeout 180 $ANDROID_HOME/platform-tools/adb shell 'while [[ -z $(getprop sys.boot_completed) ]]; do sleep 1; done' || (echo "Emulator boot timeout!" && exit 1)
        sleep 5
      # Run instrumented tests + JaCoCo coverage in single invocation
      - chmod +x gradlew
      - |
        TEST_EXIT=0
        ./gradlew --no-daemon :composeApp:connectedDebugAndroidTest :composeApp:androidConnectedTestCoverageReport || TEST_EXIT=$?
        
        # Poll for staged kover XML from build pipeline
        STAGED_KOVER="/shared/kover/${CI_COMMIT_SHA}/report.xml"
        echo "=== Polling for staged kover XML at $STAGED_KOVER ==="
        for i in $(seq 1 90); do
          if [ -f "$STAGED_KOVER" ]; then
            echo "Staged kover XML available after $((i * 10))s"
            mkdir -p composeApp/build/reports/kover
            cp "$STAGED_KOVER" composeApp/build/reports/kover/report.xml
            SKIP_KOVER_FLAG="-PskipKoverGen"
            break
          fi
          sleep 10
        done
        if [ -z "$SKIP_KOVER_FLAG" ]; then
          echo "WARNING: staged kover XML not found, will regenerate (slow)"
        fi

        # Generate unified coverage report (merges kover + jacoco)
        ./gradlew --no-daemon -PskipJaCoCoRegen $SKIP_KOVER_FLAG :composeApp:unifiedCoverageReport || true
        
        # Run koverVerify against combined coverage
        ./gradlew --no-daemon :composeApp:koverVerify || true

        # Output unified coverage
        if [ -f composeApp/build/reports/coverage/UNIFIED_COVERAGE.md ]; then
          echo "=== Unified Coverage ==="
          cat composeApp/build/reports/coverage/UNIFIED_COVERAGE.md
        fi
        
        exit $TEST_EXIT
    volumes:
      - gradle-cache-angusapp:/root/.gradle/caches
      - gradle-wrapper-angusapp:/root/.gradle/wrapper
      - android-sdk-angusapp:/opt/android-sdk
      # Read kover XML staged by build pipeline
      - kover-xml-angusapp:/shared/kover
