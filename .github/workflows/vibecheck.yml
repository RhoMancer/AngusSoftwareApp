name: vibeCheck

on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * 1'
  pull_request:
    branches: [master]

permissions:
  contents: write
  issues: write
  security-events: write
  pull-requests: write

jobs:
  analyze:
    name: Static Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run vibeCheck
        uses: RhoMancer/vibecheck@3e6a123db7f222f6dcf24efd81c9fe9f1f9f58c0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          severity_threshold: "medium"
          confidence_threshold: "medium"
          create_config_pr: "true"
