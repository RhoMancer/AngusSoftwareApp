name: vibeCheck

on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * 1'
  pull_request:
    branches: [master]

permissions:
  contents: write
  issues: write
  security-events: write
  pull-requests: write

jobs:
  analyze:
    name: Static Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run vibeCheck
        uses: RhoMancer/vibecheck@c3f45ff11f19cde0c10315a80a40d9b797869f32
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          severity_threshold: "medium"
          confidence_threshold: "medium"
          create_config_pr: "true"
