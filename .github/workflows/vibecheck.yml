name: vibeCheck

on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * 1'
  pull_request:
    branches: [master]

permissions:
  contents: write
  issues: write
  security-events: write
  pull-requests: write

jobs:
  analyze:
    name: Static Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run vibeCheck
        uses: RhoMancer/vibecheck@b19c3eaf9a2bb92a63352c09b5ab87e3aa89db12
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          severity_threshold: "medium"
          confidence_threshold: "medium"
          create_config_pr: "true"
